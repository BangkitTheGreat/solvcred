{
  "name": "solvcred",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.23.0" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --test dist/packages/core/test/*.test.js",
    "check": "npm run typecheck && npm test",
    "demo": "npm run build && node dist/scripts/demo.js",
    "benchmark": "npm run build && node dist/scripts/benchmark.js"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "typescript": "5.9.3"
  }
}
